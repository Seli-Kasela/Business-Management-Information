# Backend

This directory contains backend service(s). Two starter skeletons are included: FastAPI (Python) and Express (Node.js). Remove the one you won't use or keep both if the project will support multiple services.
